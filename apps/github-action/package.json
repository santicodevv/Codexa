{
  "name": "@codexa/github-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action codexa/audit — audita el PR y publica los resultados",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --minify",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@codexa/contracts": "0.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.14",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.5.4"
  }
}
