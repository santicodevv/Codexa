{
  "name": "@codexa/cli-core",
  "version": "0.0.0",
  "private": true,
  "description": "Helpers de reportes del CLI de Codexa",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@codexa/contracts": "0.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.14",
    "typescript": "^7.0.2"
  }
}
